<h1>Add Your Phrases</h1>

<%= form_for [@user, @phrase] do |f| %>

  <div>
    <%= f.label :phrase %>
    <%= f.hidden_field :user_id, value: current_user.id %>
    <%= f.text_field :phrase %>
  </div>
  <%= f.submit %>
<% end %>
