<h1>Update Your Address</h1>

<%= form_for @user do |f| %>

	<div>
		<%= f.label :address %>
		<%= f.text_field :address, placeholder: "Enter your address" %>
	</div>
		<%= f.submit "Update my address" %>
	<% end %>