<!DOCTYPE html>
<html>
<head>
  <title>Representin'</title>
  <link href='http://fonts.googleapis.com/css?family=Special+Elite' rel='stylesheet' type='text/css'>
  <link href='http://fonts.googleapis.com/css?family=Macondo+Swash+Caps' rel='stylesheet' type='text/css'>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  
  <%=video_tag "Movie.mp4", :size=>"100% x 540", :autoplay=>true, :loop=>true %>
 
 <div class="overlay">
   <h1>What Matters to You?</h1>
   <p>find and keep tabs on the people entrusted and elected to represent you.</p>
 </div>
	<div class ="container">
			<%if user_signed_in?%>
				<%= current_user.email %> | <%= link_to 'Sign out', destroy_user_session_path, method: 'delete'%>
				<%#= link_to 'Edit Your Phrases', new_user_phrase_path(:user_id)%> |
				<%= current_user.address %> <%= link_to 'Edit Your Location', edit_user_path(:user_id)%>
			<%else%>
				<%= link_to 'Sign in', new_user_session_path%> or
				<%= link_to 'Sign up', new_user_registration_path%>
			<%end%>
		</h4>
	</div>
	</header>
	<div class ="container">
		<p class="notice"><%= notice %></p>
	  <p class="alert"><%= alert %></p>

		<%= yield %>
	</div>
</body>
</html>
